# GitPill

MedClear — a clickable medication reminder app prototype (single-file HTML/CSS/JS).

Open `index.html` in a browser to run it. Uses the Anthropic API (client-side key entry in Settings) to read medication labels from photos.
